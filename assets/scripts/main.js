const animationLogin = bodymovin.loadAnimation({
    container: document.getElementById('svgLogin'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'assets/animations/login.json'
  });

  const animationLogin1 = bodymovin.loadAnimation({
    container: document.getElementById('svgLogin1'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'assets/animations/login.json'
  });